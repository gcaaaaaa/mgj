;(function () {
	var mySwiper1 = new Swiper('#swiper1',{
	prevButton:'.swiper-button-prev',
	nextButton:'.swiper-button-next',
	loop:true,
	autoplay:3000,
	})
	$("#swiper1").mouseenter(function () {//滑过悬停
	    mySwiper1.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper1.startAutoplay();
	});
	var mySwiper2 = new Swiper('#swiper2',{
	prevButton:'.swiper-button-prev',
	nextButton:'.swiper-button-next',
	loop:true,
	autoplay:3000,
	})
	$("#swiper2").mouseenter(function () {//滑过悬停
	    mySwiper2.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper2.startAutoplay();
	});
	var mySwiper3 = new Swiper('#swiper3',{
		pagination : '.swiper-pagination',
		paginationClickable :true,
		paginationElement : 'li',
		loop:true,
		autoplay:3000,
	})
	$("#swiper3").mouseenter(function () {//滑过悬停
	    mySwiper3.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper3.startAutoplay();
	});
	var mySwiper4 = new Swiper('#swiper4',{
		pagination : '.swiper-pagination',
		paginationClickable :true,
		paginationElement : 'li',
		loop:true,
		autoplay:3000,
	})
	$("#swiper4").mouseenter(function () {//滑过悬停
	    mySwiper4.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper4.startAutoplay();
	});
	var mySwiper5 = new Swiper('#swiper5',{
		pagination : '.swiper-pagination',
		paginationClickable :true,
		paginationElement : 'li',
		loop:true,
		autoplay:3000,
	})
	$("#swiper5").mouseenter(function () {//滑过悬停
	    mySwiper5.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper5.startAutoplay();
	});
	var mySwiper6 = new Swiper('#swiper6',{
		pagination : '.swiper-pagination',
		paginationClickable :true,
		paginationElement : 'li',
		loop:true,
		autoplay:3000,
	})
	$("#swiper6").mouseenter(function () {//滑过悬停
	    mySwiper6.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper6.startAutoplay();
	});
	var mySwiper7 = new Swiper('#swiper7',{
		pagination : '.swiper-pagination',
		paginationClickable :true,
		paginationElement : 'li',
		loop:true,
		autoplay:3000,
	})
	$("#swiper7").mouseenter(function () {//滑过悬停
	    mySwiper7.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper7.startAutoplay();
	});
	var mySwiper8 = new Swiper('#swiper8',{
		pagination : '.swiper-pagination',
		paginationClickable :true,
		paginationElement : 'li',
		loop:true,
		autoplay:3000,
	})
	$("#swiper8").mouseenter(function () {//滑过悬停
	    mySwiper8.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper8.startAutoplay();
	});
	var mySwiper9 = new Swiper('#swiper9',{
		loop:true,
		autoplay:3000,
	})
	$("#swiper9").mouseenter(function () {//滑过悬停
	    mySwiper9.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper9.startAutoplay();
	});
	var mySwiper10 = new Swiper('#swiper10',{
		loop:true,
		autoplay:3000,
	})
	$("#swiper10").mouseenter(function () {//滑过悬停
	    mySwiper10.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper10.startAutoplay();
	});
	var mySwiper11 = new Swiper('#swiper11',{
		loop:true,
		autoplay:3000,
	})
	$("#swiper11").mouseenter(function () {//滑过悬停
	    mySwiper11.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper11.startAutoplay();
	});
	var mySwiper12 = new Swiper('#swiper12',{
		loop:true,
		autoplay:3000,
	})
	$("#swiper12").mouseenter(function () {//滑过悬停
	    mySwiper12.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper12.startAutoplay();
	});
	var mySwiper13 = new Swiper('#swiper13',{
		loop:true,
		autoplay:3000,
	})
	$("#swiper13").mouseenter(function () {//滑过悬停
	    mySwiper13.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper13.startAutoplay();
	});
	var mySwiper14 = new Swiper('#swiper14',{
		loop:true,
		autoplay:3000,
	})
	$("#swiper14").mouseenter(function () {//滑过悬停
	    mySwiper14.stopAutoplay();//mySwiper 为上面你swiper实例化的名称
	}).mouseleave(function(){//离开开启
	    mySwiper14.startAutoplay();
	});
})()
          